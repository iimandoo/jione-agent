---
description: 토스 디자인을 토대로 포트폴리오 샘플페이지를 자동으로 생성합니다 (선행: /toss-theme-on → /content-setup)
argument-hint: "<페이지 이름 (선택, 기본: home)> <섹션 타입 (선택, all|hero|about|Project|contact)>"
---

🟢 **Phase 1 — 샘플 페이지 생성** (Phase 1 마지막 단계)

포트폴리오 샘플페이지를 `styled-components`로 생성합니다.

⚠️ **사전 조건**: `/project-init` → `/toss-theme-on` → `/content-setup` 순서대로 먼저 실행되어야 합니다.

## 사전 체크

아래 항목들을 확인하세요:

1. **디자인 토큰 상태 확인**
   - `jione-portfolio/src/styles/theme.ts` — styled-components 테마
   - `jione-portfolio/src/styles/provider.tsx` — ThemeProvider 설정
   - `/toss-theme-on` 명령어가 먼저 실행되었는지 확인

2. **스타일 컴포넌트 준비**
   - `jione-portfolio/src/components/sections/` 디렉토리 생성 필요
   - 각 섹션별 styled-components 파일 작성

3. **프로젝트 데이터 확인**
   - `jione-portfolio/src/data/` 또는 유사 위치의 프로젝트 데이터 파일 존재 여부

## Steps

### 1. 페이지 컴포넌트 생성

```
jione-portfolio/src/app/page.tsx 업데이트
```

- styled-components로 레이아웃 구성
- 섹션 구조: Hero → About → Project → Contact
- Responsive 디자인 적용 (mobile, tablet, desktop)

### 2. 섹션별 컴포넌트 작성 (styled-components)

#### 2-1. Hero Section

> ⚠️ Hero 섹션에는 **버튼 영역을 포함하지 않습니다**. 이름, 역할, 태그라인, 한 줄 소개만 표시합니다.

```typescript
const HeroSection = styled.section`
  min-height: 100vh;
  background: linear-gradient(135deg, #f9fafb, #ffffff);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
`;

const HeroTitle = styled.h1`
  font-size: 2.25rem;
  font-weight: 700;
  text-align: center;
  color: ${(props) => props.theme.colors.foreground};
  letter-spacing: -0.02em;

  @media (min-width: 768px) {
    font-size: 3rem;
  }
`;
```

#### 2-2. About Section

```typescript
const AboutSection = styled.section`
  padding: 5rem 1.5rem;
  background-color: #f9fafb;
`;

const Card = styled.div`
  background-color: #fff;
  padding: 1.5rem;
  border-radius: ${(props) => props.theme.radius.lg};
  box-shadow: ${(props) => props.theme.shadows.sm};
  transition: box-shadow 0.3s ease;

  &:hover {
    box-shadow: ${(props) => props.theme.shadows.md};
  }
`;
```

#### 2-3. Project Section

`resume.Project.cases`의 프로젝트를 카드 형태로 렌더링합니다.

- 레이아웃: 왼쪽 텍스트 + 오른쪽 이미지 (desktop), 세로 쌓기 (mobile)
- 이미지 1장: 단일 이미지 표시
- 이미지 2장 이상: 슬라이드 형식 (←/→ 버튼 + 하단 도트 인디케이터)
- 이미지 클릭 시 모달 팝업으로 크게 보기 (ESC·배경 클릭으로 닫기)

```typescript
'use client';

import Image from 'next/image';
import { useState, useEffect, useCallback } from 'react';
import styled, { keyframes } from 'styled-components';
import { resume } from '@/data/resume';

const Section = styled.section`
  padding: 5rem 1.5rem;
  background-color: ${(props) => props.theme.colors.muted};
`;

// 이미지 유무에 따라 flex-direction을 전환 (mobile: column, desktop: row)
const CaseCard = styled.div<{ $hasImages: boolean }>`
  background-color: ${(props) => props.theme.colors.card};
  border-radius: ${(props) => props.theme.radius.xl};
  box-shadow: ${(props) => props.theme.shadows.sm};
  overflow: hidden;
  transition: box-shadow 0.2s ease;
  display: flex;
  flex-direction: column;

  @media (min-width: 768px) {
    flex-direction: ${({ $hasImages }) => ($hasImages ? 'row' : 'column')};
  }

  &:hover {
    box-shadow: ${(props) => props.theme.shadows.md};
  }
`;

const CardBody = styled.div`
  padding: 2rem;
  flex: 1;
`;

// 오른쪽 이미지 영역
const ImageSide = styled.div`
  display: flex;
  flex-direction: column;
  border-top: 1px solid ${(props) => props.theme.colors.border};

  @media (min-width: 768px) {
    border-top: none;
    border-left: 1px solid ${(props) => props.theme.colors.border};
    flex: 0 0 45%;
  }
`;

// 단일 이미지 래퍼 — hover 시 🔍 오버레이 표시
const ImageWrapper = styled.div`
  position: relative;
  flex: 1;
  min-height: 200px;
  background-color: ${(props) => props.theme.colors.secondary};
  cursor: zoom-in;

  &:hover::after {
    content: '🔍';
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba(0, 0, 0, 0.25);
    font-size: 2rem;
  }
`;

// ─── 슬라이더 (이미지 2장 이상) ────────────────────────────────────

// overflow: hidden 으로 현재 슬라이드만 노출
const SliderContainer = styled.div`
  position: relative;
  overflow: hidden;
  flex: 1;
  min-height: 200px;
`;

// translateX 로 슬라이드 이동
const SliderTrack = styled.div<{ $index: number }>`
  display: flex;
  width: 100%;
  height: 100%;
  transform: translateX(${({ $index }) => -$index * 100}%);
  transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
`;

// 개별 슬라이드 — 클릭 시 모달
const Slide = styled.div`
  position: relative;
  flex: 0 0 100%;
  min-height: 200px;
  background-color: ${(props) => props.theme.colors.secondary};
  cursor: zoom-in;

  &:hover::after {
    content: '🔍';
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba(0, 0, 0, 0.25);
    font-size: 2rem;
  }
`;

// 이전 / 다음 버튼
const SlideNavButton = styled.button`
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  z-index: 10;
  background-color: rgba(0, 0, 0, 0.45);
  color: #fff;
  border: none;
  border-radius: 50%;
  width: 2rem;
  height: 2rem;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  font-size: 1.25rem;
  line-height: 1;
  transition: background-color 0.15s ease;

  &:hover {
    background-color: rgba(0, 0, 0, 0.7);
  }
  &[data-dir='prev'] { left: 0.5rem; }
  &[data-dir='next'] { right: 0.5rem; }
`;

// 하단 도트 인디케이터 컨테이너
const SliderDots = styled.div`
  position: absolute;
  bottom: 0.5rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 0.375rem;
  z-index: 10;
`;

// 개별 도트
const SliderDot = styled.button<{ $active: boolean }>`
  width: 6px;
  height: 6px;
  border-radius: 50%;
  border: none;
  padding: 0;
  cursor: pointer;
  background-color: ${({ $active }) =>
    $active ? 'rgba(255,255,255,1)' : 'rgba(255,255,255,0.45)'};
  transition: background-color 0.15s ease;
`;

// ─── 모달 애니메이션 ────────────────────────────────────────────────

const fadeIn = keyframes`
  from { opacity: 0; }
  to   { opacity: 1; }
`;

const scaleIn = keyframes`
  from { opacity: 0; transform: scale(0.92); }
  to   { opacity: 1; transform: scale(1); }
`;

// ─── 모달 ──────────────────────────────────────────────────────────

const ModalOverlay = styled.div`
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.88);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 2rem;
  cursor: zoom-out;
  animation: ${fadeIn} 0.2s ease;
`;

const ModalContent = styled.div`
  position: relative;
  max-width: min(90vw, 960px);
  width: 100%;
  cursor: default;
  animation: ${scaleIn} 0.25s cubic-bezier(0.16, 1, 0.3, 1);
`;

const CloseButton = styled.button`
  position: absolute;
  top: -2.75rem;
  right: 0;
  background: none;
  border: none;
  color: rgba(255, 255, 255, 0.8);
  font-size: 1.75rem;
  line-height: 1;
  padding: 0.25rem 0.5rem;
  cursor: pointer;
  transition: color 0.15s ease;

  &:hover {
    color: #ffffff;
  }
`;

const ModalImageWrapper = styled.div`
  position: relative;
  aspect-ratio: 16 / 9;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 32px 80px rgba(0, 0, 0, 0.6);
`;

// ─── 컴포넌트 ──────────────────────────────────────────────────────

interface ModalState {
  src: string;
  alt: string;
}

export function ProjectSection() {
  const { Project } = resume;
  const [modal, setModal] = useState<ModalState | null>(null);
  // 케이스별 현재 슬라이드 인덱스 { [caseId]: number }
  const [slideIndices, setSlideIndices] = useState<Record<string, number>>({});

  const closeModal = useCallback(() => setModal(null), []);

  const getIdx = (id: string) => slideIndices[id] ?? 0;

  const goSlide = useCallback((id: string, total: number, dir: 1 | -1) => {
    setSlideIndices((prev) => ({
      ...prev,
      [id]: ((prev[id] ?? 0) + dir + total) % total,
    }));
  }, []);

  // ESC 키로 닫기 + body 스크롤 잠금
  useEffect(() => {
    if (!modal) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') closeModal();
    };
    window.addEventListener('keydown', onKey);
    document.body.style.overflow = 'hidden';
    return () => {
      window.removeEventListener('keydown', onKey);
      document.body.style.overflow = '';
    };
  }, [modal, closeModal]);

  return (
    <>
      <Section id="Project">
        {Project.cases.map((c) => {
          const images = ('images' in c ? c.images : []) as readonly string[];
          return (
            <CaseCard key={c.id} $hasImages={images.length > 0}>
              {/* 왼쪽: 텍스트 */}
              <CardBody>
                {/* 텍스트 콘텐츠 */}
              </CardBody>

              {/* 오른쪽: 이미지 1장 — 단일 표시 */}
              {images.length === 1 && (
                <ImageSide>
                  <ImageWrapper
                    onClick={() => setModal({ src: images[0], alt: c.title })}
                    role="button"
                    aria-label={`${c.title} 이미지 크게 보기`}
                  >
                    <Image
                      src={images[0]}
                      alt={c.title}
                      fill
                      style={{ objectFit: 'cover' }}
                      sizes="(max-width: 768px) 100vw, 45vw"
                    />
                  </ImageWrapper>
                </ImageSide>
              )}

              {/* 오른쪽: 이미지 2장 이상 — 슬라이드 형식 */}
              {images.length >= 2 && (
                <ImageSide>
                  <SliderContainer>
                    <SliderTrack $index={getIdx(c.id)}>
                      {images.map((src, i) => (
                        <Slide
                          key={src}
                          onClick={() =>
                            setModal({ src, alt: `${c.title} ${i + 1}` })
                          }
                          role="button"
                          aria-label={`${c.title} 이미지 ${i + 1} 크게 보기`}
                        >
                          <Image
                            src={src}
                            alt={`${c.title} ${i + 1}`}
                            fill
                            style={{ objectFit: 'cover' }}
                            sizes="(max-width: 768px) 100vw, 45vw"
                          />
                        </Slide>
                      ))}
                    </SliderTrack>

                    {/* 이전 버튼 */}
                    <SlideNavButton
                      data-dir="prev"
                      onClick={(e) => {
                        e.stopPropagation();
                        goSlide(c.id, images.length, -1);
                      }}
                      aria-label="이전 이미지"
                    >
                      ‹
                    </SlideNavButton>

                    {/* 다음 버튼 */}
                    <SlideNavButton
                      data-dir="next"
                      onClick={(e) => {
                        e.stopPropagation();
                        goSlide(c.id, images.length, 1);
                      }}
                      aria-label="다음 이미지"
                    >
                      ›
                    </SlideNavButton>

                    {/* 하단 도트 */}
                    <SliderDots>
                      {images.map((_, i) => (
                        <SliderDot
                          key={i}
                          $active={getIdx(c.id) === i}
                          onClick={(e) => {
                            e.stopPropagation();
                            setSlideIndices((prev) => ({
                              ...prev,
                              [c.id]: i,
                            }));
                          }}
                          aria-label={`이미지 ${i + 1}로 이동`}
                        />
                      ))}
                    </SliderDots>
                  </SliderContainer>
                </ImageSide>
              )}
            </CaseCard>
          );
        })}
      </Section>

      {/* 이미지 모달 — 배경 클릭 또는 ESC로 닫기 */}
      {modal && (
        <ModalOverlay onClick={closeModal}>
          <ModalContent onClick={(e) => e.stopPropagation()}>
            <CloseButton onClick={closeModal} aria-label="닫기">×</CloseButton>
            <ModalImageWrapper>
              <Image
                src={modal.src}
                alt={modal.alt}
                fill
                style={{ objectFit: 'contain' }}
                sizes="min(90vw, 960px)"
                priority
              />
            </ModalImageWrapper>
          </ModalContent>
        </ModalOverlay>
      )}
    </>
  );
}
```

#### 2-4. Contact Section

> ⚠️ Contact 섹션에서 이메일과 전화번호는 **텍스트로 직접 노출**합니다 (버튼 레이블이 아닌 실제 값 표시). 클릭 시 mailto:/tel: 링크로 연결됩니다.

```typescript
const ContactInfoList = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  align-items: center;
  margin-bottom: 2rem;
`;

const ContactInfoItem = styled.a`
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 1rem;
  color: ${(props) => props.theme.colors.foreground};
  text-decoration: none;
  font-weight: 500;

  &:hover {
    color: ${(props) => props.theme.colors.primary};
  }
`;

const ContactLabel = styled.span`
  font-size: 0.875rem;
  color: ${(props) => props.theme.colors.mutedForeground};
  min-width: 3rem;
  text-align: right;
`;
```

JSX 예시:
```tsx
<ContactInfoList>
  <ContactInfoItem href={profile.social.email}>
    <ContactLabel>이메일</ContactLabel>
    {profile.email}
  </ContactInfoItem>
  <ContactInfoItem href={profile.social.phone}>
    <ContactLabel>전화</ContactLabel>
    {profile.phone}
  </ContactInfoItem>
</ContactInfoList>
```

### 3. 공통 스타일 가이드라인 (styled-components)

**타이포그래피 믹스인:**

```typescript
// src/styles/typography.ts
import styled from "styled-components";

export const H1 = styled.h1`
  font-size: 2.25rem;
  font-weight: 700;
  letter-spacing: -0.02em;
`;

export const H2 = styled.h2`
  font-size: 1.875rem;
  font-weight: 700;
  letter-spacing: -0.02em;
`;

export const Body = styled.p`
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.6;
`;

export const Small = styled.small`
  font-size: 0.875rem;
  font-weight: 400;
  color: ${(props) => props.theme.colors.mutedForeground};
`;
```

**스페이싱:**

```typescript
/* 섹션 상하 */
padding: 3rem 1.5rem; /* mobile */
padding: 5rem 2rem;   /* tablet */
padding: 7rem 3rem;   /* desktop */

/* 컨테이너 */
max-width: 64rem;
margin: 0 auto;

/* 요소 간격 */
gap: 1.5rem;
```

**컬러 팔레트 (styled-components theme):**

```typescript
${props => props.theme.colors.primary}           // #3182F6
${props => props.theme.colors.background}        // #FFFFFF
${props => props.theme.colors.muted}             // #F9FAFB
${props => props.theme.colors.foreground}        // #1B1C1F
${props => props.theme.colors.mutedForeground}   // #8B95A1
```

**쉐도우:**

```typescript
${props => props.theme.shadows.sm}   // 0 1px 4px rgba(0,0,0,0.06)
${props => props.theme.shadows.md}   // 0 4px 16px rgba(0,0,0,0.08)
${props => props.theme.shadows.lg}   // 0 8px 32px rgba(0,0,0,0.10)
```

### 4. 코드 구조 예시

```typescript
// jione-portfolio/src/app/page.tsx
'use client';

import styled from 'styled-components';
import { HeroSection } from '@/components/sections/hero';
import { AboutSection } from '@/components/sections/about';
import { ProjectSection } from '@/components/sections/Project';
import { ContactSection } from '@/components/sections/contact';

const MainContainer = styled.main`
  width: 100%;
  overflow-x: hidden;
`;

export default function Home() {
  return (
    <MainContainer>
      <HeroSection />
      <AboutSection />
      <ProjectSection />
      <ContactSection />
    </MainContainer>
  );
}
```

### 5. 섹션 컴포넌트 생성 위치

- `jione-portfolio/src/components/sections/hero.tsx`
- `jione-portfolio/src/components/sections/about.tsx`
- `jione-portfolio/src/components/sections/Project.tsx` — `resume.Project.cases` + 이미지 사이드 패널 (`next/image`) + 클릭 모달 팝업
- `jione-portfolio/src/components/sections/contact.tsx`

> `Project.tsx`에서 이미지는 `public/images/project/` 경로를 사용합니다. 이미지 파일을 해당 경로에 추가하세요.

### 6. 빌드 및 검증

```bash
cd jione-portfolio && npm run build
npm run dev  # 로컬 확인
```

페이지가 정상 렌더링되고, 토스 디자인이 일관되게 적용되었는지 확인

### 7. 완료 보고

- 생성된 파일 목록
- 적용된 섹션
- 빌드 성공 여부

## Rules

- **스타일 관리**: 모든 스타일은 `styled-components` 사용
- **테마 일관성**: `src/styles/theme.ts`의 토큰만 사용
- **콘텐츠 보존**: 기존 프로젝트 데이터는 수정하지 않음 (읽기만 수행)
- **반응형**: mobile-first 방식으로 확실하게 개발 (`@media` 쿼리 필수)
- **성능**: 이미지는 `next/image`로 최적화
- **접근성**: Semantic HTML 사용, 적절한 heading 계층 유지
- **빌드**: 생성 후 반드시 `npm run build` 실행하여 에러 확인
- **아이콘 및 커서 리소스**: Google Fonts 아이콘 폰트(예: Material Symbols/Icons) 사용. `cursor`에 사용하는 이미지나 아이콘도 모두 폰트 기반으로 처리해야 하며, `layout.tsx` `<head>`에 CDN 링크를 추가한다.
  ```html
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet" />
  ```
  사용법: `<span className="material-symbols-outlined">icon_name</span>` 혹은 필요 시 커서 스타일에 동일한 폰트에서 글리프를 참조하여 커서 이미지를 생성한다.
- **이미지 커서**: `cursor: zoom-in` 사용 금지. 이미지 hover 효과는 subtle한 dim overlay(`rgba(0,0,0,0.08)`)로만 표현
- **애니메이션**: Framer Motion 사용 (이미 설치됨)
  - `motion.div` + `whileInView` + `viewport={{ once: true }}`로 스크롤 진입 시 fade-up
  - variants는 반드시 `Variants` 타입으로 명시 (`import { motion, type Variants } from 'framer-motion'`)
  - ease는 `'easeOut'` 문자열 대신 `Variants` 타입 선언 시 자동 추론됨
  - Hero: `animate` (페이지 로드 시), 나머지 섹션: `whileInView` (스크롤 진입 시)
  - 카드 목록은 `custom` 인덱스로 stagger delay 적용

## 완료 보고

```
🎉 Phase 1 완료!

✅ /project-init       — 프로젝트 초기화
✅ /toss-theme-on      — 토스 디자인 시스템 적용
✅ /content-setup      — resume.ts 콘텐츠 입력
✅ /sample-page-create — 단일 페이지 섹션 완성

→ http://localhost:3000 에서 포트폴리오 확인
```

## 다음 단계

### 🔵 Phase 2 (선택) — 준비가 되면 진행

```
5️⃣ /seo-optimize          ← SEO 메타데이터, sitemap, robots.txt
   ↓
6️⃣ /vercel-deploy         ← Vercel 배포 (GitHub 자동 배포)
   ↓
7️⃣ /admin-logging-setup   ← 방문자 분석 (/admin 대시보드)
   ↓
8️⃣ /monitoring-setup      ← Google Analytics, 성능 모니터링
```

> Phase 2는 순서대로 진행하지 않아도 됩니다. 필요한 항목만 선택하여 실행 가능합니다.
