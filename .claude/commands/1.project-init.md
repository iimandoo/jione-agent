---
description: 토스 스타일 포트폴리오를 위한 Next.js 프로젝트 초기화 (첫 번째 실행)
argument-hint: "<프로젝트 이름 (선택, 기본: jione-portfolio)>"
---

🟢 **Phase 1 — 프로젝트 초기화** (필수)

Next.js 16 + TypeScript 기반 포트폴리오 프로젝트를 완전히 초기화합니다.
이 command는 **가장 먼저 실행되어야 합니다**.

## 목표

- Next.js 16 + React 19 프로젝트 구조 생성
- 필수 의존성 설치 (Tailwind, shadcn/ui, Framer Motion 등)
- 폴더 구조 자동 생성
- 환경 변수 템플릿 생성
- TypeScript, ESLint, Prettier 설정

## Steps

### 1. 프로젝트 생성 (Vercel에서 제공하는 공식 템플릿 사용)

```bash
npx create-next-app@latest jione-portfolio --typescript

cd jione-portfolio
```

설정 선택사항:

```
✔ TypeScript: Yes
✔ ESLint: Yes
✔ Tailwind CSS: No
✔ App Router: Yes
✔ Src/ directory: Yes
✔ Import alias: Yes (@/* = src/*)
```

### 2. 필수 의존성 설치

```bash
npm install \
  framer-motion \
  react-query \
  recoil \
  clsx \
  styled-components \
  next-image-export-optimizer \
  web-vitals

# styled-components 타입 정의 (개발용)
npm install --save-dev @types/styled-components
```

### 3. 폴더 구조 자동 생성

생성할 폴더:

```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                   # 단일 페이지 포트폴리오 메인 (모든 섹션 포함)
│   ├── admin/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   └── api/
│       └── admin/
│           └── logs/
│               ├── route.ts
│               └── clear/
│                   └── route.ts
├── components/
│   ├── sections/
│   │   ├── hero.tsx
│   │   ├── about.tsx
│   │   ├── projects.tsx
│   │   └── contact.tsx
│   ├── ui/
│   │   ├── button.tsx (shadcn)
│   │   ├── card.tsx (shadcn)
│   │   └── badge.tsx (shadcn)
│   └── page-logger.tsx
├── data/
│   └── resume.ts                # 모든 포트폴리오 콘텐츠의 단일 소스 (경력, 프로젝트, 기술, SEO)
├── lib/
│   ├── db.ts                    # 데이터베이스 (로깅)
│   └── memory-db.ts             # 메모리 DB (In-Memory 저장)
├── actions/
│   └── logging.ts               # Server Actions
├── styles/
│   └── globals.css              # 전역 CSS (토스 디자인 토큰)
└── middleware.ts                # 페이지 로그 감지

public/
├── robots.txt                   # SEO
├── sitemap.xml                  # SEO
└── images/                      # 이미지 저장소
```

#### 3-1. 핵심 파일 생성

**`src/middleware.ts`** 생성 (최소 버전):

```typescript
import { NextRequest, NextResponse } from "next/server";

export function middleware(request: NextRequest) {
  if (request.nextUrl.pathname.startsWith("/admin")) {
    return NextResponse.next();
  }

  if (request.nextUrl.pathname.startsWith("/_next")) {
    return NextResponse.next();
  }

  return NextResponse.next();
}

export const config = {
  matcher: ["/((?!api|_next/static|_next/image|favicon.ico).*)"],
};
```

**`src/lib/memory-db.ts`** 생성:

```typescript
interface PageLog {
  id: number;
  url: string;
  path: string;
  referrer?: string;
  userAgent?: string;
  ipAddress?: string;
  timestamp: Date;
}

let logs: PageLog[] = [];
let nextId = 1;

export const memoryDB = {
  addLog: (data: Omit<PageLog, "id" | "timestamp">) => {
    const log: PageLog = {
      id: nextId++,
      ...data,
      timestamp: new Date(),
    };
    logs.push(log);
    return log;
  },
  getLogs: (limit: number = 100) => {
    return logs
      .sort((a, b) => b.timestamp.getTime() - a.timestamp.getTime())
      .slice(0, limit);
  },
  getStats: () => {
    const stats: Record<string, { count: number; lastVisit: Date }> = {};
    logs.forEach((log) => {
      if (!stats[log.path])
        stats[log.path] = { count: 0, lastVisit: log.timestamp };
      stats[log.path].count++;
      if (log.timestamp > stats[log.path].lastVisit) {
        stats[log.path].lastVisit = log.timestamp;
      }
    });
    return stats;
  },
};
```

**`src/actions/logging.ts`** 생성:

```typescript
"use server";

import { cookies } from "next/headers";
import { memoryDB } from "@/lib/memory-db";

export async function logPageView() {
  try {
    const cookieStore = await cookies();
    const pageViewData = cookieStore.get("_pageViewData")?.value;
    if (!pageViewData) return;

    const data = JSON.parse(pageViewData);
    memoryDB.addLog({
      url: data.url,
      path: data.path,
      referrer: data.referrer,
      userAgent: data.userAgent,
      ipAddress: data.ipAddress,
    });
  } catch (error) {
    console.error("Failed to log page view:", error);
  }
}
```

### 4. 환경 변수 템플릿 생성 및 styled-components 설정

**`next.config.ts`** 업데이트 (styled-components 네이티브 컴파일러 활성화):

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  compiler: {
    styledComponents: true,
  },
};

export default nextConfig;
```

> ⚠️ Next.js 16에서는 `.babelrc` 대신 `next.config.ts`의 `compiler.styledComponents`를 사용합니다. Babel 사용 시 Turbopack이 비활성화되어 빌드가 느려집니다.

**`.env.local.example`** 생성:

```env
# SEO & Metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Your Portfolio

# Analytics (선택)
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# Database (선택)
DATABASE_URL=

# Admin (선택, 보안용)
ADMIN_PASSWORD=
```

실제 `.env.local` 파일 생성 (git에 추가하지 않음):

```bash
cp .env.local.example .env.local
# .env.local 파일 수동 편집
```

### 5. TypeScript & ESLint 설정 확인

**`tsconfig.json`** 확인:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "jsx": "preserve",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "allowJs": true,
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**`.eslintrc.json`** 설정 (기본값 사용):

```json
{
  "extends": "next/core-web-vitals"
}
```

### 6. Prettier 설정 (선택)

**`.prettierrc`** 생성:

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "arrowParens": "always"
}
```

### 7. 기본 페이지 생성

**`src/app/layout.tsx`** 업데이트:

```typescript
import type { Metadata } from 'next';
import '@/styles/globals.css';
// StyleProvider는 /toss-theme-on 실행 후 추가됩니다

export const metadata: Metadata = {
  title: '포트폴리오',
  description: 'Full-Side Engineer 포트폴리오',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css" rel="stylesheet" />
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
```

**`src/app/page.tsx`** 생성:

```typescript
import styled from 'styled-components';

const MainContainer = styled.main`
  min-height: 100vh;
  background-color: #ffffff;
`;

const PlaceholderSection = styled.section`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  gap: 1rem;
`;

const Title = styled.h1`
  font-size: 2.25rem;
  font-weight: bold;
  text-align: center;
`;

const Description = styled.p`
  text-align: center;
  color: #666666;
`;

// /toss-theme-on, /sample-page-create 실행 후 아래 섹션 컴포넌트로 교체됩니다.
// import HeroSection from '@/components/sections/hero';
// import AboutSection from '@/components/sections/about';
// import ProjectsSection from '@/components/sections/projects';
// import ContactSection from '@/components/sections/contact';

export default function Home() {
  return (
    <MainContainer>
      <PlaceholderSection>
        <Title>포트폴리오</Title>
        <Description>
          /toss-theme-on 명령어를 실행하여 디자인을 적용하세요.
        </Description>
      </PlaceholderSection>
      {/* 섹션 컴포넌트 생성 후 순서대로 추가:
      <HeroSection />
      <AboutSection />
      <ProjectsSection />
      <ContactSection />
      */}
    </MainContainer>
  );
}
```

### 8. 빌드 및 검증

```bash
npm run build
npm run dev
```

- http://localhost:3000 접속하여 정상 작동 확인
- 터미널에서 빌드 에러 확인 및 수정

### 9. Git 초기화 (선택, Vercel 배포를 위함)

```bash
git init
git add .
git commit -m "Initial Next.js portfolio setup

- Next.js 16 with App Router
- TypeScript configuration
- Tailwind CSS + shadcn/ui
- Memory database for logging
- Ready for Toss Design System
"

# GitHub 저장소에 푸시
git remote add origin <your-github-repo-url>
git branch -M main
git push -u origin main
```

## Rules

- **패키지 매니저**: npm 사용 (yarn, bun도 가능하지만 일관성 유지)
- **Node 버전**: 18.17 이상 권장
- **빌드 성공**: 반드시 `npm run build` 실행 후 에러 없음 확인
- **파일 구조**: 위 구조를 정확히 따를 것 (다음 commands가 의존함)
- **환경 변수**: `.env.local`은 git 추적 대상에서 제외 (`.gitignore`에 추가)

## 완료 보고

초기화 완료 후:

```
✅ Next.js 프로젝트 생성
✅ 필수 의존성 설치
✅ 폴더 구조 자동 생성
✅ 환경 변수 템플릿 생성
✅ TypeScript/ESLint 설정 완료
✅ npm run build 성공

다음 명령어: /toss-theme-on
```

## 다음 단계

### 🟢 Phase 1 (필수) — 계속 진행

1. **`/toss-theme-on`** → 디자인 시스템 적용
2. **`/content-setup`** → resume.ts 콘텐츠 입력
3. **`/sample-page-create`** → 섹션 컴포넌트 생성

> Phase 1 완료 후 로컬에서 포트폴리오 확인 가능

### 🔵 Phase 2 (선택) — 나중에 진행

4. **`/seo-optimize`** → SEO 메타데이터 설정
5. **`/vercel-deploy`** → Vercel 배포
6. **`/admin-logging-setup`** → 방문자 분석
7. **`/monitoring-setup`** → 성능 모니터링
